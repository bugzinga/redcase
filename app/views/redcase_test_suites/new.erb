var form = "<%= raw (render "form").gsub(/[\n]+/, "").gsub(/["]+/, "\\\"") %>";
<% if flash[:error] %>
    $('errors').update('<%= flash[:error] %>');
<% else %>
    $('modalDiv').insert(form);
<% end %>
showModal('modalDiv');