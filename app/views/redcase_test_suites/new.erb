var form = "<%= raw (render "form_create").gsub(/[\n]+/, "").gsub(/["]+/, "\\\"") %>";

<% if flash[:error] %>
    $('errors').update('<%= flash[:error] %>');
<% else %>
    $('modalDiv').insert(form);
    showModal('modalDiv');
<% end %>
